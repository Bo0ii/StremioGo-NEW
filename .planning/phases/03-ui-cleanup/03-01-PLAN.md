---
phase: 03-ui-cleanup
plan: 01
type: execute
depends_on: []
files_modified: [src/components/plus-page/plus-page.html]
---

<objective>
Remove native player sidebar item from Plus Page HTML template.

Purpose: Clean up UI navigation to remove MPV integration access point after core files deleted in Phase 2.
Output: Plus Page template with native player sidebar item removed, other categories intact.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-investigation-mapping/01-02-SUMMARY.md
@.planning/phases/02-core-file-removal/02-03-SUMMARY.md
@src/components/plus-page/plus-page.html

**Prior phase context:**
Phase 2 removed all core MPV files (MpvManager.ts, NativePlayerConfig.ts, Anime4KProfiles.ts, component directories). Now removing UI access points.

**Target integration point:**
The native player sidebar item in plus-page.html (lines 81-86) provides navigation to the settings page. This must be removed to eliminate user access to the now-deleted MPV integration.

**Established patterns:**
- HTML templates use kebab-case naming
- Templates are copied from src/components/ to dist/components/ via copyComponents.js
- Plus Page uses sidebar navigation with data-category attributes
- Other categories (themes, plugins, tweaks, appearance, about) must remain intact
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove native player sidebar item from HTML template</name>
  <files>src/components/plus-page/plus-page.html</files>
  <action>
Delete lines 81-86 in src/components/plus-page/plus-page.html:
```html
<div class="plus-sidebar-item" data-category="nativeplayer">
    <svg viewBox="0 0 24 24" width="20" height="20" style="fill: currentColor;">
        <path d="M8 5v14l11-7z"/>
    </svg>
    <span>Native Player</span>
</div>
```

DO NOT remove any other sidebar items (themes, plugins, tweaks, appearance, about). The native player item is the 5th item, positioned between "Appearance" (lines 75-80) and "About" (lines 87-92).

WHY: This sidebar item navigates to data-category="nativeplayer" which triggers getNativePlayerContent() in plusPage.ts. With core MPV files deleted, this category will fail to render properly. Removing now prevents broken UI state.
  </action>
  <verify>
grep -n "nativeplayer" src/components/plus-page/plus-page.html returns no matches.
grep -c "plus-sidebar-item" src/components/plus-page/plus-page.html returns 5 (was 6, now 5 remaining categories).
  </verify>
  <done>
- Lines 81-86 deleted from plus-page.html
- No "nativeplayer" references in HTML template
- 5 sidebar items remain (themes, plugins, tweaks, appearance, about)
- File still syntactically valid HTML
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify HTML template integrity</name>
  <files>src/components/plus-page/plus-page.html</files>
  <action>
Read the modified plus-page.html file and verify:
1. All remaining sidebar items have proper opening/closing div tags
2. The sidebar navigation structure is intact (div.plus-sidebar-nav still contains 5 items)
3. No orphaned tags or broken syntax from deletion
4. File ends with proper closing tags (</style>, </div>)

This is a sanity check to ensure the deletion didn't introduce HTML syntax errors.
  </action>
  <verify>
Read src/components/plus-page/plus-page.html - file parses as valid HTML with 5 sidebar items present.
  </verify>
  <done>
- HTML template is syntactically valid
- Sidebar navigation contains exactly 5 items
- No orphaned tags or syntax errors
- Template ready for compilation
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -i "nativeplayer\|native-player\|native player" src/components/plus-page/plus-page.html` returns no matches
- [ ] HTML file is syntactically valid (no broken tags)
- [ ] 5 sidebar items remain (themes, plugins, tweaks, appearance, about)
</verification>

<success_criteria>

- Native player sidebar item removed from HTML template
- All verification checks pass
- No HTML syntax errors introduced
- Other sidebar categories intact
- Template compiles without errors (will verify in Plan 02 after TypeScript changes)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-cleanup/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Native Player Sidebar Item Removal Summary

**Removed native player navigation item from Plus Page HTML template**

## Accomplishments

- Deleted lines 81-86 from src/components/plus-page/plus-page.html (native player sidebar item)
- Verified HTML template integrity after deletion
- Reduced sidebar navigation from 6 items to 5 items

## Files Created/Modified

- `src/components/plus-page/plus-page.html` - Removed native player sidebar item (6 lines deleted)

## Decisions Made

None - straightforward deletion as planned.

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for **03-02-PLAN.md** - Remove native player logic from plusPage.ts (getNativePlayerContent function, setupNativePlayerControls function, category registration)
</output>
